/**
 * Created by wangjianwen on 2017/10/16.
 */
import * as func from "../function";
export default {
  setTableList(states, tableList) {
    states.tableList = tableList;
    // func.func.set('tableList',tableList);
  },
  setInventoryPeriod(states, inventoryInfo) {
    states.inventoryInfo = inventoryInfo;
    func.func.set('inventoryInfo',inventoryInfo);
  },
  setInventoryList(states, inventoryList) {
    states.inventoryList = inventoryList;
    func.func.set('inventoryList',inventoryList);
  },
  setApplyParam(states, applyParam) {
    states.applyParam = applyParam;
    func.func.set('applyParam',applyParam);
  },
  setUid(states, u_id) {
      states.u_id = u_id;
      func.func.set('u_id',u_id);
  },
  setEditId(states, edit_id) {
     states.edit_id = edit_id;
     func.func.set('edit_id',edit_id);
  },
};
